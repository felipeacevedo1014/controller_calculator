Important:
- Fix resultion issues: Modify image size depending on the user screen resolution
- Total VA requirements

Good to have:


Implemented updates:
- Progress indicator during calculations
- Error handler: Limitation in the points capacity per controller, wainign to the user before executing the code. Include spare points in that calcualtion
- Include PM014 checkbox
- Datasheets
- Tooltips
- Add the S800 as a controller
- Add/Delete row
- Template download

pyinstaller app.spec --clean --noconfirm

pip install -U pyinstaller packaging requests certifi
pyinstaller --noconfirm --onefile --name controller_calculator gui.py



